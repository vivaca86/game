# Vertical Slice Smoke Checklist v1

Date: 2026-05-26

## Status

- Status: combat simulation, scene flow, click controls, save reload, 1920/view smoke, and card effect audit verified.
- Basis: `docs/vertical-slice-acceptance.md`
- Candidate data: `docs/vertical-slice-content-candidates.md`
- Still not complete: full vertical-slice pass, final art/assets, exhaustive source parity, 95% similarity.

## State Values

| State | Meaning |
| --- | --- |
| Not started | No implementation or verification yet. |
| Implemented, not verified | Code exists, but the specific acceptance check has not been proven. |
| Needs fix | Verification found a problem. |
| Verified | The stated check has verification evidence. |
| Blocked | Tool, source, or decision gap blocks progress. |

## Pre-Run Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| PRE-001 | Phaser app runs through Vite. | `npm.cmd run dev`, `npm.cmd run phaser:smoke` | Verified |
| PRE-002 | TypeScript has no errors. | `npm.cmd run check` | Verified |
| PRE-003 | Runtime data loads. | `BootScene`, `npm.cmd run phaser:smoke` | Verified |
| PRE-004 | Asset manifest has no missing references. | `npm.cmd run slice:validate` | Verified |
| PRE-005 | Debug entry flags exist. | `?debug=1&entry=combat`, `?debug=1&entry=boss` | Verified |

## Data Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| DATA-001 | All content has `id`. | `npm.cmd run slice:validate` | Verified |
| DATA-002 | All displayed content has Korean name/description fields. | `npm.cmd run slice:validate` | Verified |
| DATA-003 | All slice content has `referenceRole`. | `npm.cmd run slice:validate` | Verified |
| DATA-004 | All `referenceRole` values are mapped in `reference-role-map-slice-v1.md`. | `npm.cmd run slice:validate` | Verified |
| DATA-005 | All referenced asset keys exist in the generated manifest. | `npm.cmd run slice:validate` | Verified |
| DATA-006 | Card descriptions and effect ops do not contradict each other. | `npm.cmd run slice:effects` | Verified |
| DATA-007 | Save data does not store Phaser objects. | `npm.cmd run phaser:smoke` save snapshot check | Verified |
| DATA-008 | Room `encounterPoolId` values reference explicit encounter-pool data, and pool entries point to the right content domain. | `npm.cmd run slice:validate` + `npm.cmd run phaser:smoke` | Verified |
| DATA-009 | Generated asset manifest stays in sync with runtime manifest, strict file audit passes, and Phaser preloads runtime asset URLs. | `npm.cmd run assets:audit:strict` + `npm.cmd run phaser:smoke` asset response check | Verified |
| DATA-010 | Full local catalog can be loaded through `?data=release` without replacing the verified slice default. | `npm.cmd run phaser:smoke` release catalog case | Verified |
| DATA-011 | Full release visual candidate manifest covers the local catalog without orphan or missing runtime files. | `npm.cmd run assets:audit:release-visuals` + `npm.cmd run assets:audit` + `npm.cmd run phaser:smoke` | Verified |

## UI Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| UI-001 | Town screen shows start/progress state. | 1920 screenshot | Verified |
| UI-002 | World map can enter the selected stage. | keyboard flow | Verified |
| UI-003 | Dungeon view is not only a static menu and shows route position. | 1920 screenshot + interaction smoke | Verified |
| UI-004 | Combat shows hand, HP, energy, block, and intent. | `npm.cmd run phaser:smoke` | Verified |
| UI-005 | Reward screen shows options and claim result. | flow smoke + 1920 screenshot | Verified |
| UI-006 | Event screen shows event text, choices, costs, and rewards. | flow smoke + 1920 screenshot + release event screenshot | Verified |
| UI-007 | Rune bench shows equip result before/after. | flow smoke + 1920 screenshot | Verified |
| UI-008 | Boss screen shows boss-specific phase signal. | `npm.cmd run phaser:smoke` | Verified |
| UI-009 | Result screen shows clear/return state. | full loop smoke | Verified |
| UI-010 | Settings screen exposes audio, display, accessibility, controls, and reset options. | `npm.cmd run phaser:smoke` settings surface case | Verified |

## Combat Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| COMBAT-001 | `card_sun_jab` damages the front enemy. | debug combat overlay: `enemyHp=17` after Digit1 | Verified |
| COMBAT-002 | `card_fold_guard` gives block. | debug combat overlay: `playerBlock=6` after Digit2 | Verified |
| COMBAT-003 | `card_page_step` draws a card. | debug combat overlay: `drawPile=0`, `discard=1`, hand updated | Verified |
| COMBAT-004 | Enemy attack intent deals damage after end turn. | debug combat overlay: `playerHp=36`, `turn=2` after `E` | Verified |
| COMBAT-005 | Combat log and state agree with the action result. | debug overlay `log=...` plus smoke assertions | Verified |
| COMBAT-006 | Equipped rune changes attached-card value. | flow smoke: `rune_paper_spark` makes `card_sun_jab` deal 9 | Verified |
| COMBAT-007 | Boss phase triggers at HP threshold. | boss debug smoke: `bossPhaseTriggered=true` | Verified |
| COMBAT-008 | `card_lamplight_mark` applies mark and the next attack consumes it as bonus damage. | debug combat smoke: `enemyMark=2`, then `enemyMark=0` and `enemyHp=15` | Verified |

## Loop Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| LOOP-001 | Town enters stage flow. | full loop smoke | Verified |
| LOOP-002 | First combat victory enters reward. | full loop smoke | Verified |
| LOOP-003 | Reward claim enters rune bench. | full loop smoke | Verified |
| LOOP-004 | Rune equip affects the next combat. | full loop smoke | Verified |
| LOOP-005 | Boss clear creates result state. | boss loop smoke | Verified |
| LOOP-006 | Result can return to town. | boss loop smoke | Verified |
| LOOP-007 | Minimum progress survives save reload. | `npm.cmd run phaser:smoke` + in-app browser reload check | Verified |

## Responsive/View Checks

| ID | Check | Method | Current state |
| --- | --- | --- | --- |
| VIEW-001 | 1920x1080 layout keeps five cards and intent readable. | 1920 screenshot + smoke assertions | Verified |
| VIEW-002 | 1280x720 layout renders nonblank without console errors. | `npm.cmd run phaser:smoke` | Verified |
| VIEW-003 | Browser console has no app errors. | `npm.cmd run phaser:smoke` | Verified |
| VIEW-004 | Debug overlay does not hide critical combat information. | 1920/1280/1080 overlay geometry + screenshot review | Verified |

## Current Verification Results

- `npm.cmd run slice:validate`: passed.
- `npm.cmd run slice:effects`: passed.
- `npm.cmd run assets:audit`: passed in generated-manifest mode.
- `npm.cmd run check`: passed.
- `npm.cmd run phaser:smoke`: passed.
- `git diff --check`: passed.
- Save reload/reset/recovery: `phaser:smoke` verifies saved mid-combat state restores after reload, completed-stage profile state survives reload, v0/unversioned saves migrate to v1 with invalid fields normalized, corrupt JSON and unsupported future versions recover to a valid initial save, clear and defeat result states persist consistently, debug reset restores an initial profile, and the Town `저장 초기화` action deletes the production save key back to an initial profile.
- Effect audit: `slice:effects` verifies docs/runtime fixture card effect text stays in sync, implemented card descriptions do not keep draft `후보` wording, each card effect op has a Korean description cue/amount, and each op is handled by the Phaser slice simulation.
- Encounter pool refs: `slice:validate` verifies route `encounterPoolId` values point to `encounterPools[].id`, pool type matches the room type, pool entries reference the right enemy/event/boss domain, and the boss pool includes the stage `bossId`.
- Mark smoke: `phaser:smoke` uses `?debug=1&entry=combat&resetSave=1&grantCard=card_lamplight_mark`, confirms mark becomes `enemyMark=2`, then the next attack consumes mark to `enemyMark=0` and reduces enemy HP from 24 to 15.
- 1920/view checks: `phaser:smoke` now captures 1920x1080 screenshots for Town, Dungeon, Combat, Reward, Rune Bench, and Boss under `tmp/phaser-1920-*.png`. Combat and Boss assertions verify five-card hands, expected debug state, non-empty screenshots, and debug-overlay avoidance of the hand and enemy intent areas.
- Responsive overlay checks: `phaser:smoke` also captures 1280 and 1080 overlay screenshots for Combat and Boss under `tmp/phaser-overlay-*.png`. In-app browser verification at 1080x918 confirmed `phase=combat`, `enemyHp=24`, `playerEnergy=3`, overlay size `220x260`, and no overlap with the five-card hand or enemy intent panel.
- Asset file audit: `assets:audit` verifies the docs manifest and runtime manifest stay synchronized, asset keys/paths remain unique, generated PNG files under `public/assets/runtime` match `nativeSize`, and these files are still development placeholders rather than final art. `phaser:smoke` verifies the first page preloads all 40 slice `assets/runtime` URLs without browser errors.
- 2026-05-27 card UI candidate pass: the generated development art for `card_frame_attack`, `card_frame_defense`, `card_frame_skill`, `icon_card_attack`, `icon_card_defense`, and `icon_card_skill` was replaced with more specific deterministic candidate art. This is a first replacement-loop proof, not approved final art.
- 2026-05-27 card art render pass: the first hand card illustrations `card_art_sun_jab`, `card_art_fold_guard`, `card_art_page_step`, `card_art_ribbon_snap`, and `card_art_lamplight_mark` were replaced with deterministic candidate art, and the Phaser hand renderer now draws card frame, illustration, and type icon textures instead of rectangle-only cards. This is still candidate/development art.
- 2026-05-27 remaining card art candidate pass: the remaining slice card illustrations `card_art_stage_patch`, `card_art_ink_spill`, `card_art_paper_bloom`, `card_art_pinpoint_glint`, and `card_art_curtain_call` were replaced with deterministic candidate art. Separate `grantCard` screenshots verified these remaining cards render through the existing hand texture path. This completes the first candidate pass for all 10 slice card illustrations, but not final approved art.
- 2026-05-27 intent icon candidate pass: `icon_intent_attack`, `icon_intent_disrupt`, and `icon_intent_block` were replaced with deterministic candidate icons, and the Combat/Boss intent panel now renders the active intent texture from `assetKeys.intentIcons[]`. Additional turn-two screenshots verified the disrupt and block slots. This is still candidate/development art.
- 2026-05-27 background candidate pass: `bg_lantern_foyer_set` and `scene_rune_bench` were replaced with deterministic candidate backgrounds. Shared Phaser scenes now render the current stage `assetKeys.backgroundSet`, and Rune Bench resolves its event scene asset from the event encounter pool. Combat and Rune Bench screenshots verified that the backgrounds render behind readable UI. This is still candidate/development art.
- 2026-05-27 monster sprite pipeline placeholder pass: `monster_folded_sentry` and `monster_ink_mote` were replaced with deterministic transparent spritesheet placeholders, and Combat now renders the active regular enemy texture from `enemy.assetKeys.sprite`. Default combat and `enemy=enemy_ink_mote` screenshots verified both regular monster sprites render. This is a pipeline placeholder pass, not a visual candidate or final sprite pass.
- 2026-05-27 boss sprite pipeline placeholder pass: `boss_curtain_lion` was replaced with a deterministic transparent spritesheet placeholder, and Boss now renders the active boss texture from `boss.assetKeys.sprite` through the shared combat panel. Boss screenshot review also tightened the Boss End Turn button position. This is a pipeline placeholder pass, not a visual candidate or final boss art pass.
- 2026-05-27 progression icon candidate pass: `map_icon_lantern_foyer`, three rune icons, `relic_brass_bookmark_icon`, and `char_mina_pagehand_portrait` were replaced with deterministic candidate art. Shared scene shells now render the character portrait and stage map icon, Reward renders reward-type icons, Rune Bench renders rune icons, and Result can render collected relic icons through a debug-granted relic screenshot. This is still candidate/development art, not final approved art.
- 2026-05-27 effect spritesheet pipeline pass: `effect_paper_slash`, `effect_ink_splash`, and `effect_stage_spotlight` were replaced with deterministic transparent spritesheet candidates. Combat/Boss now resolve a feedback effect from recent combat log state, render the relevant spritesheet frame, expose `effect=` in the debug overlay, and smoke captures paper-slash, ink-splash, and boss-stage spotlight screenshots. This is a feedback pipeline proof only, not final VFX timing or polish.
- 2026-06-01 character spritesheet pipeline pass: `char_mina_pagehand_sprite` was replaced with a deterministic transparent spritesheet candidate, and Combat/Boss now render it as the player-side standee from `character.assetKeys.sprite`. Smoke checks `characterSprite=char_mina_pagehand_sprite` and captures character-sprite screenshots for Combat and Boss. This proves the starter character sprite path only, not final animation or approved character art.
- 2026-06-01 UI panel 9-slice pass: `ui_panel_paper_9slice` was replaced with a deterministic candidate, and the shared scene status panel plus Combat/Boss enemy panel now render it through Phaser `nineslice`. Smoke checks `uiPanel=ui_panel_paper_9slice` and captures UI-panel screenshots for Combat and Boss. This proves the panel texture path only, not final UI polish or button-state skinning.
- 2026-06-01 input gate audit: `phaser:smoke` now verifies mouse/click action paths for Town, WorldMap, Dungeon, Combat, Reward, Event, RuneBench, Boss, and Result, plus release event/reward click paths. Keyboard actions remain covered through the existing Enter/Space, Digit1-5, and E smoke flow. The smoke also pixel-checks hover feedback for shared action buttons, combat cards, Combat/Boss End Turn, Event choices, and RuneBench action, writing `tmp/phaser-hover-*.png` screenshots. This proves the current Phaser gameplay/menu input gate, not final accessibility settings or full UI skinning.
- 2026-06-01 text quality gate audit: fixed Phaser screen chrome now uses Korean labels instead of the old English placeholders. `phaser:smoke` exposes the Phaser game for layout inspection and checks visible text objects for scene bounds, severe text overlap, replacement characters, and old English placeholder labels across Town, WorldMap, Dungeon, Combat, Reward, Event, RuneBench, Boss, and Result. This proves current screen text fit/consistency only; it is not final copywriting approval for every content row.
- 2026-06-01 core run loop gate audit: `phaser:smoke` now plays a release `stage_sunny_gate` run from Town through repeated combat rooms, Event, elite combat, Reward, rest/RuneBench, Boss, stage-clear reward, Result, and Town return, verifying `saveCompleted=stage_sunny_gate`. The smoke also verifies a defeat path from 1 HP to Result with `playerHp=0`, no completed-stage credit, Town return, and new world-map start. This proves the current core loop, not final balance or full manual QA.
- 2026-06-01 save system gate audit: `phaser:smoke` now covers debug and production save keys. It verifies mid-combat reload, completed-stage reload, legacy v0/unversioned migration to v1, invalid-field normalization, corrupt JSON recovery, unsupported future-version reset, clear-result save consistency, defeat-result save consistency, debug reset, and Town `저장 초기화` production-save deletion. This proves the current durable save/load/reset/recovery gate, not broader progression, settings, cloud save, or long-term unlock balance.
- 2026-06-01 settings gate audit: `phaser:smoke` enters Settings from Town, verifies default master/music/SFX volume values, Korean language status, standard display mode, large-text/reduced-motion/Space-confirm defaults, changes all mutable settings, verifies the saved JSON shape and values, reloads to prove persistence, confirms Space no longer advances Town while `spaceConfirm=false`, resets settings to defaults, and confirms Space advances again after reset. This proves the current settings surface only; it does not complete music/SFX assets or sound design.
- 2026-06-01 release catalog runtime bridge: `?data=release` now loads the full local catalog through `src/data/releaseCatalogAdapter.ts` while the default smoke path stays on the verified slice fixture. `phaser:smoke` verifies `dataMode=release`, `cards=113`, `runesTotal=58`, `relicsTotal=16`, `stagesTotal=15`, `validation=ok`, the first release enemy, the first release hand, and `card_sunbean_punch` reducing `enemy_cloud_buddy` from 22 HP to 14 HP. Screenshot: `tmp/phaser-release-catalog-combat.png`. This proves full-catalog runtime connectivity only; release content quality remains tracked in `docs/release-readiness-checklist.md`.
- 2026-06-01 card release batch 1: 32 generated cards across the morning/cloud/mint/peach families were converted into distinct card ids, names, effect structures, text, and illustration briefs. `cards:release:audit` verifies the full 113-card file, confirms the retired template ids are gone, checks release card ops, and caps repeated visible-card effect structures at 9 and suffix repetition at 8. `quality:audit:report` no longer reports the previous card repetition blockers. This is card release progress, not final card-set completion.
- 2026-06-01 gem release batch 1: 20 generated gems across the morning/cloud/mint/peach/lavender edge/guard/spark/echo groups were converted into distinct multi-effect socket modifiers with Korean text. `gems:release:audit` verifies the full 58-gem file, curated batch coverage, and max repeated gem effect shape 9. `runtime:smoke` verifies `gem_morning_edge` damage plus preserve-chain behavior in the core runtime, and `phaser:smoke` verifies the same gem as a release rune reduces `enemy_cloud_buddy` from 22 HP to 12 after `card_sunbean_punch`. `quality:audit:report` no longer reports the previous gem repetition blocker. This is gem release progress, not final gem/rune completion.
- 2026-06-01 enemy intent release batch 1: 32 generated enemies across early normal/trick/elite/boss rows were converted into distinct intent structures. `enemies:release:audit` verifies the full 60-enemy file, curated batch 32, unique batch shapes 20, and max repeated intent shape 7. `phaser:smoke` verifies release enemy debuff handling through `enemy_cloud_trick` setting `playerMark=1` and release special handling through `enemy_ribbon_buddy` setting `nextCardCostPenalty=1`. `quality:audit:report` no longer reports the previous enemy intent-pattern blocker. This is enemy intent progress, not final enemy or boss completion.
- 2026-06-01 enemy identity release batch 2: the remaining 28 generated enemy rows were curated, early boss phase labels were replaced, and the enemy quality gate was changed from an unconditional blocker into explicit release criteria. `enemies:release:audit` verifies `curatedCoverage=60/60`, `uniqueIntentShapes=37`, `maxRepeatedIntentShape=5`, all 7 supported special effects, both debuff statuses, and `bossPhaseCoverage=15/15`. `runtime:smoke` verifies a batch 2 weak intent and temp-card enemy behavior, and `phaser:smoke` verifies `enemy_prism_trick` sets `playerWeak=1` in `?data=release`. `quality:audit:report` no longer reports the enemy generated-coverage blocker. This is enemy identity progress, not final enemy art, balance, encounter pacing, or boss completion.
- 2026-06-01 stage route release batch 1: all 15 generated stage room patterns were curated into distinct room orders while preserving event rooms, elite rooms, and final boss rooms. `stages:release:audit` verifies `uniqueRoomPatterns=15` and `maxRepeatedRoomPattern=1`. `phaser:smoke` verifies `stage_rainbow_keep` in `?data=release` exposes `route=combat>elite>event>combat>shop>rest>elite>reward>event>rest>boss`. `quality:audit:report` no longer reports the previous repeated stage room-pattern blocker. This is stage route progress, not final stage completion.
- 2026-06-01 event release batch 1: all 10 generated events were curated into varied choice counts, labels, cost profiles, and reward profiles. `events:release:audit` verifies `events=10`, `totalChoices=36`, choice-count distribution `4:6, 3:4`, `choiceRoles=7`, and `risks=4`. `runtime:smoke` now selects affordable event choices instead of assuming choice 0 is always valid, and `phaser:smoke` verifies `event_bubble_shop` in `?data=release` exposes `eventChoices=4`, `eventCosts=spend_currency|spend_currency|free|spend_hp`, and `eventRewards=card|rune+heal|currency|relic+currency`. `quality:audit:report` no longer reports the previous event choice-shape blocker. This is event data/adapter progress, not final event-system completion.
- 2026-06-01 achievement release batch 1: 31 generated `ach_picnic_goal_*` rows were converted from room-count filler into collection and chain mastery goals. `achievements:release:audit` verifies `achievements=161`, `curatedBatch=31`, `milestoneLike=84`, five batch trigger families, and seven batch reward kinds. `runtime:smoke` verifies curated card/gem/relic/arcana collection achievements and chain achievements apply unlock rewards, including relic and gem reward bridges. `quality:audit:report` no longer reports the previous achievement bulk-trigger blocker. This is achievement curation progress, not final achievement/progression completion.
- 2026-06-01 release visual coverage batch 1: `tools/generate-release-visual-assets.mjs` generated 363 candidate PNGs and a synchronized release manifest pair for the full local catalog. `assets:audit:release-visuals` verifies `assets=363`, `expectedAssets=363`, and `existingFiles=363`. `assets:audit` verifies 403 total slice+release runtime files, 0 missing files, and 0 orphan files. `phaser:smoke` verifies the release catalog still renders in browser, with screenshot `tmp/phaser-release-catalog-combat.png`. `quality:audit:report` now reports no current automated quality blockers. This is release visual candidate coverage, not final approved art or full visual QA.
- 2026-06-01 EventScene execution batch 1: Phaser now has an `event` phase and dedicated `EventScene`. Smoke captures `tmp/phaser-1920-EventScene.png` for the slice event and `tmp/phaser-event-release-bubble-shop.png` for release `event_bubble_shop`. Release smoke verifies choice 3 starts `enemy_cloud_buddy` combat and changes gold to 104, while choice 4 spends HP to 60, grants `relic_round_lantern`, and changes gold to 92. This is event execution progress, not final event balance, repeat-state handling, or full event-system completion.
- 2026-06-01 relic/arcana passive execution batch 1: `phaser:smoke` verifies release passive state changes through debug grants. Relic checks cover `relic_round_lantern` start block 4, `relic_cloud_socks` start energy 4 total, `relic_mint_thermos` after-combat heal from 60 to 64, `relic_ribbon_box` card reward option count 4, and `relic_candy_pouch` event gold reaching 109. Arcana checks cover `arcana_star_bakery` increasing gold to 82 after a zero-cost card and `arcana_cloud_parade` reducing enemy HP to 20 after a defense card. This is passive batch progress, not full relic/arcana completion.
- 2026-06-01 relic/arcana passive execution batch 2: `phaser:smoke` now also verifies `relic_sun_cookie` first 2-cost card free, `relic_sleepy_pillow` one-card retain, `relic_bubble_lens` rune reward option count, `relic_elite_sticker` mixed elite card reward, `relic_final_picnic` stage-clear reward option count plus actual boss-victory reward transition, `relic_soft_compass` next-room reveal, `arcana_ribbon_firework` five-card damage, `arcana_mint_rest` three-defense heal, `arcana_prism_path` four-color draw, `arcana_cloud_blanket` block carry, and `arcana_sprout_song` heal-trigger mark. This is passive batch progress, not full relic/arcana completion.
- 2026-06-01 UI technical skin path audit: added 9-slice candidate assets for `ui_button_primary_9slice`, `ui_button_secondary_9slice`, `ui_slot_reward_9slice`, `ui_slot_choice_9slice`, and `ui_tooltip_paper_9slice`, alongside the existing shared panel. `phaser:smoke` verifies `uiSkin=button+slot+tooltip`, hover/down pixel changes, Settings control hover, disabled release event choice non-advancement at 1 HP, and screenshots `tmp/phaser-ui-skin-settings-controls.png` plus `tmp/phaser-ui-skin-event-disabled-choice.png`. Manual user review on 2026-06-01 rejected the visible result as final UI. This proves the runtime skinning path only; concept/source-backed, art-bible-faithful `UI skin` remains `Not done`.
- 2026-06-01 UI concept rebuild checkpoint 1: created `assets/concepts/ui/reward_event_ui_concept_v001.png` and rebuilt Reward/Event toward the popup-book paper-stage direction. Reward choices now render as stage cards and clicking/number-selecting a reward claims that specific reward. Event choices now render as paper choice cards, and a text-overlap smoke failure was fixed by removing the overlapping status badge. `phaser:smoke`, `npm.cmd run check`, and `git diff --check` pass. This is improvement evidence only; `docs/ui-visual-quality-rubric.md` still scores Reward 60/100 and Event 58/100, so `UI skin` remains `Not done`.
- 2026-06-01 UI concept rebuild checkpoint 2: Reward cards were enlarged, reward details were added, Event gained a story diorama with scene art plus denser choice cards, and the shared immersive shell/background was kept in the paper-theater direction. Debug-less screenshots were captured at `tmp/ui-quality/reward-immersive-chrome-v3.png` and `tmp/ui-quality/event-immersive-chrome-v3.png`. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Reward 70/100 and Event 68/100, below the 85 critical-screen red line.
- 2026-06-01 UI concept rebuild checkpoint 3: added source component evidence at `assets/source/ui/reward_event_stage_components_v001.svg`, removed extra immersive info chrome from Reward/Event, and strengthened Event choice cards with clearer cost/reward/selection bands. Debug-less screenshots were captured at `tmp/ui-quality/reward-immersive-chrome-v4.png`, `tmp/ui-quality/event-immersive-chrome-v4.png`, `tmp/ui-quality/reward-1920-debugless-v4.png`, and `tmp/ui-quality/event-1920-debugless-v4.png`. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Reward 76/100 and Event 75/100, below the 85 critical-screen red line.
- 2026-06-01 UI Combat rebuild checkpoint 1: added source component evidence at `assets/source/ui/combat_stage_components_v001.svg`, rebuilt Combat into the same immersive paper-theater direction, moved the player status panel and route strip away from the title ribbon, and kept card/end-turn input coordinates smoke-compatible. Debug-less screenshots were captured at `tmp/ui-quality/combat-1920-debugless-v1.png` and `tmp/ui-quality/combat-1920-debugless-v2.png`. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Combat 72/100, below the 85 critical-screen red line.
- 2026-06-01 UI Combat rebuild checkpoint 2: added v2 source component evidence at `assets/source/ui/combat_stage_components_v002.svg`, added side paper wings and hanging pins, converted the player panel into stat tags, strengthened the route strip connector, and added a combat-flow lane without moving the smoke-covered card/end-turn click zones. Debug-less screenshots were captured at `tmp/ui-quality/combat-1920-debugless-v3.png` and `tmp/ui-quality/combat-1920-debugless-v4.png`; v3 showed hidden central text and out-of-panel gold, then v4 fixed those placement defects. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Combat 78/100, below the 85 critical-screen red line.
- 2026-06-01 UI Event rebuild checkpoint 4: added dedicated v2 source component evidence at `assets/source/ui/event_stage_components_v002.svg`, added side paper wings, strengthened the story diorama label band, replaced the meta helper line with in-world flavor text, refined choice cards with cost/result rows, and captured both slice and release four-choice debug-less screenshots. Release screenshot v5 exposed a confirm-button collision with the bottom choice row; v6 fixed the collision by moving and reducing the confirm button. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Event 81/100, below the 85 critical-screen red line.
- 2026-06-01 UI Reward rebuild checkpoint 5: added dedicated v2 source component evidence at `assets/source/ui/reward_stage_components_v002.svg`, rebuilt Reward around a centered prize-stage header, input badge, reward shelf, stronger reward cards, type ribbons, medallions, icon stamps, detail wells, footer action strips, and default-selection tag. Debug-less screenshots cover slice Reward, release card rewards, release rune rewards, and 1280x720 layouts under `tmp/ui-quality/reward-*-v6.png`. Screenshot v5 exposed English release reward-pool titles and an obscured shelf label; v6 fixed both. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Reward 85/100, while Event and Combat remain below the 85 critical-screen red line.
- 2026-06-02 UI Combat rebuild checkpoint 3: added dedicated v3 source component evidence at `assets/source/ui/combat_stage_components_v003.svg`, removed the duplicate runtime overlay approach, rebuilt Combat player/enemy information as readable ledgers, added route label backplates, and preserved card/end-turn input coordinates. Debug-less screenshots now cover `tmp/ui-quality/combat-1920-debugless-v7.png`, `tmp/ui-quality/combat-1280-debugless-v7.png`, `tmp/ui-quality/combat-release-1920-debugless-v7.png`, and `tmp/ui-quality/boss-combat-panel-1920-debugless-v7.png`. The first v5 review exposed duplicate/bleed-through issues, and the first smoke rerun caught a long release enemy name overlapping HP text; both were fixed before final verification. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Combat 85/100, while Event remains below the 85 critical-screen red line and the total UI gate remains below 95.
- 2026-06-02 UI Event rebuild checkpoint 5: added dedicated v3 source component evidence at `assets/source/ui/event_stage_components_v003.svg`, kept the Event diorama/stage direction, tightened the release four-choice layout with compact reward summaries, and separated cost/result label chips from values. Debug-less screenshots now cover `tmp/ui-quality/event-1920-debugless-v8.png`, `tmp/ui-quality/event-1280-debugless-v8.png`, `tmp/ui-quality/event-release-1920-debugless-v8.png`, and `tmp/ui-quality/event-release-1280-debugless-v8.png`. The first v7 review exposed cost/value crowding, then v8 fixed the label/value separation. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Event 85/100, and Reward/Event/Combat are internal 85-point candidates, not final 95-point UI completion.
- 2026-06-02 UI Boss rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/boss_stage_components_v001.svg`, moved Boss into the same source-backed paper-theater family as Combat, added boss-specific purple/brass accents and a phase ledger, and compacted boss intent labels so release boss text does not crowd the End Turn area. Debug-less screenshots now cover `tmp/ui-quality/boss-1920-debugless-v4.png`, `tmp/ui-quality/boss-1280-debugless-v4.png`, and `tmp/ui-quality/boss-release-1920-debugless-v4.png`. The first smoke pass exposed boss phase ledger overlap with hand-card numbers, and screenshot review exposed long release intent crowding; both were fixed before the v4 evidence set. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Boss 84/100, not final 95-point UI completion.
- 2026-06-02 UI Town rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/town_stage_components_v001.svg`, moved Town into a source-backed paper-theater village hub, and preserved the smoke-covered world-map, save-reset, and settings click coordinates. Debug-less screenshots now cover `tmp/ui-quality/town-1920-debugless-v4.png`, `tmp/ui-quality/town-1280-debugless-v4.png`, and `tmp/ui-quality/town-release-1920-debugless-v4.png`. The first screenshot passes exposed route-button overlap, crowded village/passport text, and shortcut-like tooltip copy; v4 fixes those issues. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Town 83/100, not final 95-point UI completion.
- 2026-06-02 UI WorldMap rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/world_map_stage_components_v001.svg`, moved WorldMap into a source-backed unfolded paper-map stage, and preserved the smoke-covered `던전으로` click coordinate. Debug-less screenshots now cover `tmp/ui-quality/world-map-1920-debugless-v3.png`, `tmp/ui-quality/world-map-1280-debugless-v3.png`, and `tmp/ui-quality/world-map-release-1920-debugless-v3.png`. The first screenshot passes exposed right-ledger/tooltip crowding, route nodes too close to the record panel, and non-in-world helper copy; v3 fixes those issues while leaving the 1280 layout tight. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores WorldMap 82/100, not final 95-point UI completion.
- 2026-06-02 UI Dungeon rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/dungeon_stage_components_v001.svg`, moved Dungeon into a source-backed paper corridor stage, and preserved the smoke-covered `방 입장` click coordinate at `1010,582`. Debug-less screenshots now cover `tmp/ui-quality/dungeon-1920-debugless-v3.png`, `tmp/ui-quality/dungeon-1280-debugless-v3.png`, and `tmp/ui-quality/dungeon-release-1920-debugless-v3.png`. The first screenshot passes exposed route-helper crowding, and the first post-rebuild smoke run exposed the room number overlapping the `방 입장` button; v3 fixes both while leaving the 1280 layout tight. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Dungeon 82/100, not final 95-point UI completion.
- 2026-06-02 UI RuneBench rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/rune_bench_stage_components_v001.svg`, moved RuneBench into a source-backed socket workbench, and preserved the smoke-covered `룬 장착` click coordinate at `1010,742`. Debug-less screenshots now cover `tmp/ui-quality/rune-bench-1920-debugless-v3.png`, `tmp/ui-quality/rune-bench-1280-debugless-v3.png`, and `tmp/ui-quality/rune-bench-release-1920-debugless-v3.png`. The first screenshot passes exposed the action button covering the target-card preview, and v2 exposed raw internal effect op text; v3 fixes both while leaving the 1280 layout tight. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores RuneBench 82/100, not final 95-point UI completion.
- 2026-06-02 UI Result rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/result_stage_components_v001.svg`, moved Result into a source-backed curtain-call certificate, and preserved the smoke-covered `마을로` click coordinate at `1010,742`. Debug-less screenshots now cover `tmp/ui-quality/result-1920-debugless-v1.png`, `tmp/ui-quality/result-1280-debugless-v1.png`, `tmp/ui-quality/result-defeat-1920-debugless-v1.png`, `tmp/ui-quality/result-clear-1920-debugless-v1.png`, and `tmp/ui-quality/result-release-clear-1920-debugless-v1.png`. The defeat and clear captures were driven through real combat/boss result paths before hiding the debug overlay for visual evidence. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Result 83/100, not final 95-point UI completion.
- 2026-06-02 UI Settings rebuild checkpoint 1: added dedicated source component evidence at `assets/source/ui/settings_stage_components_v001.svg`, moved Settings into a source-backed settings ledger, and preserved the smoke-covered volume, display/accessibility toggle, reset, and return click coordinates. Debug-less screenshots now cover `tmp/ui-quality/settings-1920-debugless-v2.png`, `tmp/ui-quality/settings-1280-debugless-v2.png`, `tmp/ui-quality/settings-release-1920-debugless-v2.png`, and `tmp/ui-quality/settings-high-contrast-1920-debugless-v2.png`. The first v1 screenshot pass exposed oversized row labels and border-crowded headings; v2 fixes the typography while leaving the 1280 layout compact. Verification passed with `npx.cmd tsc --noEmit`, `npm.cmd run phaser:smoke`, `npm.cmd run check`, `npm.cmd run data:validate`, `npm.cmd run quality:audit:report`, `npm.cmd run assets:audit`, `npm.cmd run assets:audit:release-visuals`, and `git diff --check`. This is still improvement evidence only; the rubric scores Settings 84/100, not final 95-point UI completion.

- 2026-06-02 UI WorldMap rebuild checkpoint 2: added dedicated v2 source component evidence at `assets/source/ui/world_map_stage_components_v002.svg`, changed the map drawer into save-backed current/complete, selectable, and next-locked rows, and added release smoke coverage for clear -> next-stage unlock -> stage selection. `phaser:smoke` verifies `unlockedStages=stage_sunny_gate,stage_lavender_hall`, clicking the second map row, `stage=stage_lavender_hall`, and `log=flow:stage_select:stage_lavender_hall`. Debug-less screenshots now cover `tmp/ui-quality/world-map-1920-debugless-v5.png`, `tmp/ui-quality/world-map-1280-debugless-v5.png`, `tmp/ui-quality/world-map-release-1920-debugless-v5.png`, `tmp/ui-quality/world-map-release-unlocked-1920-debugless-v5.png`, and `tmp/ui-quality/world-map-release-selected-1920-debugless-v5.png`. This is still improvement evidence only; the rubric scores WorldMap 86/100, not final 95-point UI completion or full world-atlas completion.

- 2026-06-02 UI Dungeon rebuild checkpoint 2: added dedicated v2 source component evidence at `assets/source/ui/dungeon_stage_components_v002.svg`, changed Dungeon into a state-driven room-progression surface, and surfaced current-room, next-room intel, boss-distance, resource, and route-state information from the active run. Debug-less screenshots now cover `tmp/ui-quality/dungeon-1920-debugless-v5.png`, `tmp/ui-quality/dungeon-1280-debugless-v5.png`, `tmp/ui-quality/dungeon-release-1920-debugless-v5.png`, and `tmp/ui-quality/dungeon-release-compass-1920-debugless-v5.png`. v4 exposed the next-room intel card overlapping the route panel; v5 fixes that overlap. `phaser:smoke` also writes `tmp/phaser-dungeon-release-soft-compass.png` while verifying `relic_soft_compass` next-room reveal. This is still improvement evidence only; the rubric scores Dungeon 85/100, not final 95-point UI completion or full spatial dungeon navigation completion.

- 2026-06-02 UI RuneBench rebuild checkpoint 2: added dedicated v2 source component evidence at `assets/source/ui/rune_bench_stage_components_v002.svg`, changed RuneBench into a state-driven socket preview, and surfaced selected-rune, target-card, before/after stat, compatibility, recommendation, and change-row information from the active run. Debug-less screenshots now cover `tmp/ui-quality/rune-bench-1920-debugless-v5.png`, `tmp/ui-quality/rune-bench-1280-debugless-v5.png`, and `tmp/ui-quality/rune-bench-release-1920-debugless-v5.png`. The first v4 smoke rerun exposed a decorative `룬` label overlapping the socket label; v5 fixes that overlap. This is still improvement evidence only; the rubric scores RuneBench 85/100, not final 95-point UI completion or full rune selection/socket progression completion.

- 2026-06-02 UI Town rebuild checkpoint 2: added dedicated v2 source component evidence at `assets/source/ui/town_stage_components_v002.svg`, changed Town into a save/profile-driven expedition board, and surfaced unlocked/completed/remaining counts, next expedition, completed-stage stamps, route preview, and next-sealed status from runtime state. Debug-less screenshots now cover `tmp/ui-quality/town-1920-debugless-v8.png`, `tmp/ui-quality/town-1280-debugless-v8.png`, `tmp/ui-quality/town-release-1920-debugless-v8.png`, and `tmp/ui-quality/town-release-progress-1920-debugless-v8.png`. v5/v6/v7 exposed route-card and next-sealed label crowding, then v8 fixed the visible placement defects. This is still improvement evidence only; the rubric scores Town 86/100, not final 95-point UI completion or Town/meta system completion.

- 2026-06-02 UI Result rebuild checkpoint 2: added dedicated v2 source component evidence at `assets/source/ui/result_stage_components_v002.svg`, changed Result into a state-driven run recap with route progress, route composition, next record, deck delta, and preservation summary, and preserved the smoke-covered `마을로` click coordinate at `1010,742`. Debug-less screenshots now cover `tmp/ui-quality/result-1920-debugless-v5.png`, `tmp/ui-quality/result-1280-debugless-v5.png`, `tmp/ui-quality/result-defeat-1920-debugless-v5.png`, `tmp/ui-quality/result-clear-1920-debugless-v5.png`, and `tmp/ui-quality/result-release-clear-1920-debugless-v5.png`. v2 hid the next-action ticket under the collection ledger, v3 was too cramped, and v4 collided with the result title; v5 fixes those visible placement defects. This is still improvement evidence only; the rubric scores Result 86/100, not final 95-point UI completion or ending/progression completion.

`phaser:smoke` writes screenshots under `tmp/`, including `tmp/phaser-TownScene.png`, `tmp/phaser-CombatScene.png`, and `tmp/phaser-BossScene.png`. `tmp/` is verification output and is not committed.

## Pass Judgment

The first vertical-slice smoke checklist is verified: all listed `PRE`, `DATA`, `UI`, `COMBAT`, `LOOP`, and `VIEW` rows are `Verified`, no `Needs fix` rows remain, and screenshot/save-reload/effect-audit evidence is recorded. The release catalog bridge and release visual candidate coverage are also smoke/audit-verified as separate connectivity and coverage checks, not as release completion.

This is still not a production-complete game, final art/assets pass, exhaustive source parity check, or original-game 95% similarity claim.

## Next Work

1. Keep `?data=release` smoke green while release-facing systems are implemented against the full catalog.
2. Continue card, gem, enemy, stage, event, and achievement work beyond the first release batches; `quality:audit:report` currently has no automated blockers, but domain release gates still need art approval, balance, UI, pacing, and manual play/readability review.
3. Continue final art/assets, balance, and UX polish only with explicit evidence; do not use slice/candidate proof as release completion.
4. Keep Steam/appmanifest direct proof deferred unless the user later provides access or exact build/runtime claims become necessary.
