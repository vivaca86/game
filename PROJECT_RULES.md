# 프로젝트 최상위 규칙

이 문서는 이 저장소에서 진행하는 모든 작업보다 우선하는 운영 규칙이다.
기능 개발, 문서 작성, 검증, 커밋, 푸시, 로컬 정리, 대화 기록, 문제 해결 기록은 아래 규칙을 기준으로 진행한다.

## 0. 에이전트 운영 기준

Codex가 실제로 따라야 하는 작업 운영 기준은 `AGENTS.md`의 `Authoritative English Rules` 섹션이다.
`AGENTS.md`의 한글 섹션은 사용자를 위한 참고 번역이며, 영문과 한글 표현이 다르면 영문 규칙을 우선한다.

- 완료, 부분 완료, 검증 미완료, 조사 미완료, 차단됨 같은 상태 구분은 `AGENTS.md` 기준을 따른다.
- 삭제, 로컬 정리, Git 조작, 구조 변경, 대규모 개발, 전수조사, 95%/완벽 판정은 실행 전 범위와 위험을 사용자에게 제시한다.
- 사용자가 요청한 범위를 Codex가 조용히 줄이거나 넓히거나 재해석하지 않는다.
- 전수조사, 완벽, 95%, 원작과 거의 동일한 구현은 체크리스트 없이 시작하지 않는다.
- 실제 상태가 예상과 다르면 계속 진행하지 않고 보고한다.

## 1. 작업 루트 정리

작업 종료 후 지정된 로컬 작업 루트 내부는 반드시 정리하고, 로컬에는 최종 작업 흔적을 남기지 않는다.

- 작업 루트는 현재 환경에서 사용자가 지정했거나 Codex가 작업 중인 최상위 작업 폴더를 뜻한다.
- 작업 루트 경로는 PC와 세션마다 달라질 수 있으므로, 규칙 문서에는 특정 절대경로를 고정하지 않는다. Codex는 각 작업 환경에서 실제 작업 루트를 확인한 뒤 이 규칙을 적용한다.
- 작업 종료는 사용자가 명확하게 선언한 경우에만 해당하며, Codex는 로컬 정리 또는 삭제 작업을 진행하기 전에 반드시 한 번 더 확인한다.
- 사용자가 작업 종료를 확인하면, GitHub에 커밋/푸시된 최종 결과물을 제외하고 작업 루트 내부의 로컬 프로젝트 폴더, 임시 파일, 빌드 산출물, 스크린샷, 로그, 캐시 등 작업 흔적 전체를 삭제 대상으로 본다.
- 삭제 대상에는 현재 작업 중인 저장소 폴더 자체도 포함될 수 있으며, 최종 보존 기준은 로컬 파일이 아니라 GitHub에 반영된 커밋이다.
- 공용 PC 또는 외부 환경에서 여러 프로젝트를 진행할 수 있으므로, 작업 루트 내부에는 다음 작업과 혼동될 수 있는 로컬 흔적을 남기지 않는다.

## 2. 대화 원문 로그 관리

Codex와의 대화 내용은 히스토리 보존을 위해 대화 로그 파일에 원문 그대로 기록한다.

- 대화 로그는 요약본이 아니라, 사용자의 요청과 Codex의 답변을 가능한 한 원문 그대로 남기는 기록용 파일이다.
- 대화 로그 파일은 `docs/conversation-log.md`로 관리한다.
- 이후 작업자가 당시의 맥락, 판단 과정, 문제 제기, 결정 사항을 직접 확인할 수 있도록 관리한다.
- 대화 로그는 로컬에만 두지 않고 GitHub 저장소에 커밋/푸시된 파일로 관리한다.
- 계정 정보, API 키, 비밀번호, 개인 인증 정보 같은 민감 정보는 기록하지 않는다.

## 3. 문제점과 해결 방안 로그 관리

작업 중 발견된 문제점과 해결 방안은 대화 로그와 분리해 별도 문제 해결 로그 파일로 관리한다.

- 문제 해결 로그 파일은 `docs/problem-resolution-log.md`로 관리한다.
- 작업 중 발견된 문제점, 반복되는 실패 원인, 품질 리스크, 해결 방안, 재발 방지 기준을 기록한다.
- 문제 해결이 완료되면 어떤 작업 또는 커밋으로 해결했는지 기록한다.
- 문제 해결 로그는 단순 감상이나 변명이 아니라, 이후 작업에서 같은 문제가 반복되지 않도록 확인 가능한 기준으로 작성한다.
- 문제 해결 로그도 로컬에만 두지 않고 GitHub 저장소에 커밋/푸시된 파일로 관리한다.
